export default interface UpdateCustomer {
        id: number,

        firstName?: string,
        lastName?: string,
        email?: string,
        phone?: string,
        address?: string,
    
   
}